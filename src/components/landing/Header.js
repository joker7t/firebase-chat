import React from 'react';
import style from './scss/Header.module.scss';

const Header = () => {
	return (
		<div className={style.Header}>
			<h1>Download the app right now!</h1>
		</div>
	);
};

export default Header;
